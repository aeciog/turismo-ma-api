version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: turismo-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: turismo_ma
      MYSQL_USER: turismo
      MYSQL_PASSWORD: turismo123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  redis:
    image: redis:7.2
    container_name: turismo-redis
    ports:
      - "6379:6379"

volumes:
  mysql-data: